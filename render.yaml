services:
  - type: web
    name: constellation-prototype
    runtime: python
    plan: free
    buildCommand: pip install -e .
    startCommand: uvicorn constellation_core.api:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /health
    autoDeploy: true
